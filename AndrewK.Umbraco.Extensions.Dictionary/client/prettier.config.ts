import type { Config } from 'prettier'

const config: Config = {
  semi: false,
  singleQuote: true,
  trailingComma: 'none',
  arrowParens: 'avoid',
  printWidth: 120,
  tabWidth: 2,
  htmlWhitespaceSensitivity: 'ignore'
}

export default config
